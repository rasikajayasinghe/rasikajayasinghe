"use client";
import { PortableText } from "@portabletext/react";

interface BlogTestProps {
  postBody: [object];
}

const BlogText = ({ postBody }: BlogTestProps) => {
  return <div>test</div>;
};

export default BlogText;
